/**!
 * 
 * Admin Scripts
 * @since 1.0.0
 * @version 1.0.0
 * @author 
 * @copyright 
 */
;(function ($, window, document, configs) {
    "use strict";
    const __variables__ = '';
    $(document)
        .on('ready', function () {});
    $(window)
        .on('load', function () {})
        .on('resize', function () {})
        .trigger('resize');
})(jQuery, window, document, configs || {});
